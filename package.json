{
  "name": "cuet-mock-workspace",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install-all": "npm install --prefix backend && npm install --prefix frontend",
    "seed": "npm run seed --prefix backend",
    "backend": "npm start --prefix backend",
    "frontend": "npm run dev --prefix frontend",
    "dev": "npx concurrently --kill-others \"npm start --prefix backend\" \"npm run dev --prefix frontend\"",
    "host": "npx concurrently --kill-others \"npm start --prefix backend\" \"npm run dev --prefix frontend -- --host\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
